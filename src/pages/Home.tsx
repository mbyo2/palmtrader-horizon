
import Index from "./Index";

// This is a simple wrapper to maintain the existing route structure
// while using the enhanced Index component
const Home = () => {
  return <Index />;
};

export default Home;
